# 工作流示例

本文档提供真实场景下使用云效 Bug 修复技能的详细示例。

## 示例 1：处理直接 Bug 链接

**用户输入：**
```
请修复这个 bug：https://devops.aliyun.com/projex/project/xxx/bug/TXRP-592
```

**Agent 工作流：**

1. **提取 Bug ID**：从 URL 解析得到 `TXRP-592`

2. **初始化环境**：
   ```javascript
   const { organizationId, userId } = await mcp_aliyun_yunxiao_get_current_organization_info()
   ```

3. **获取 Bug 详情**：
   ```javascript
   const bug = await mcp_aliyun_yunxiao_get_work_item({ 
     organizationId, 
     workItemId: "TXRP-592" 
   })
   ```

4. **展示供确认**：
   ```markdown
   ## 🐛 Bug 详情
   
   **Bug ID:** TXRP-592
   **标题:** 移动端登录按钮无响应
   **状态:** 待处理 | **优先级:** 高
   
   **问题描述：**
   在移动设备（iOS Safari）上点击登录按钮时，
   没有任何反应。桌面端正常。
   
   **是否开始修复此 Bug？**
   - 回复"确认"继续
   ```

5. **等待用户确认**：用户回复"确认"

6. **获取完整上下文**：
   - 获取评论
   - 获取附件列表
   - 下载截图

7. **定位代码**：搜索登录按钮组件

8. **修复并展示**：展示修复计划，等待最终确认

9. **更新云效**：用户确认后，更新状态并添加评论

## 示例 2：搜索"我的 Bug"

**用户输入：**
```
显示我的待处理 bug
```

**Agent 工作流：**

1. **初始化环境**：
   ```javascript
   const { organizationId, userId } = await mcp_aliyun_yunxiao_get_current_organization_info()
   ```

2. **搜索工作项**：
   ```javascript
   const bugs = await mcp_aliyun_yunxiao_search_workitems({
     organizationId,
     category: "Bug",
     assignedTo: "self",
     statusStage: "1,2", // 排除已关闭
     includeDetails: true,
     orderBy: "priority",
     sort: "desc"
   })
   ```

3. **展示 Bug 列表**：
   ```markdown
   ## 🐛 你的待处理 Bug（共 3 个）
   
   ### TXRP-592 - 登录按钮无响应
   **优先级:** 高 | **状态:** 待处理
   
   ### TXRP-593 - 头像上传失败
   **优先级:** 中 | **状态:** 进行中
   
   ### TXRP-594 - 错误提示文案错误
   **优先级:** 低 | **状态:** 待处理
   
   **请选择要修复的Bug：**
   回复 Bug 编号（如 "TXRP-592"）
   ```

4. **等待用户选择**：用户回复"修复 TXRP-592"

5. **继续标准流程**：同示例 1，从步骤 6 开始

## 示例 3：高优先级 Bug

**用户输入：**
```
有哪些高优先级的 bug 需要关注？
```

**Agent 工作流：**

1. **初始化环境**

2. **搜索并过滤**：
   ```javascript
   const bugs = await mcp_aliyun_yunxiao_search_workitems({
     organizationId,
     category: "Bug",
     statusStage: "1,2",
     includeDetails: true,
     orderBy: "priority",
     sort: "desc",
     perPage: 10
   })
   ```

3. **过滤高优先级**：筛选优先级为"高"的 Bug

4. **展示列表**：展示高优先级 Bug 及详情

5. **等待用户操作**：用户可以选择一个修复或仅查看

## 示例 4：最近创建的 Bug

**用户输入：**
```
显示最近 3 天创建的 bug
```

**Agent 工作流：**

1. **初始化环境**

2. **计算日期范围**：获取 3 天前的时间戳

3. **搜索并排序**：
   ```javascript
   const bugs = await mcp_aliyun_yunxiao_search_workitems({
     organizationId,
     category: "Bug",
     statusStage: "1,2",
     includeDetails: true,
     orderBy: "gmtCreate",
     sort: "desc",
     perPage: 50
   })
   ```

4. **按日期过滤**：筛选最近 3 天创建的 Bug

5. **展示列表**：展示最近的 Bug

6. **等待用户操作**

## 示例 5：关键词搜索

**用户输入：**
```
查找与"支付"相关的 bug
```

**Agent 工作流：**

1. **初始化环境**

2. **关键词搜索**：
   ```javascript
   const bugs = await mcp_aliyun_yunxiao_search_workitems({
     organizationId,
     category: "Bug",
     statusStage: "1,2",
     includeDetails: true,
     subjectDescription: "支付", // 在标题和描述中搜索
     orderBy: "gmtCreate",
     sort: "desc"
   })
   ```

3. **展示结果**：展示所有支付相关的 Bug

4. **等待用户选择**

## 示例 6：处理附件

**用户输入：**
```
修复 TXRP-595
```

**Agent 工作流（步骤 3 - 获取上下文）：**

1. **获取工作项详情**

2. **获取评论**

3. **获取附件列表**：
   ```javascript
   const attachments = await mcp_aliyun_yunxiao_list_workitem_attachments({
     organizationId,
     workItemId: "TXRP-595"
   })
   // 返回: [{ id: "123", name: "screenshot.png", ... }]
   ```

4. **下载每个截图**：
   ```javascript
   for (const attachment of attachments) {
     const { downloadUrl } = await mcp_aliyun_yunxiao_get_workitem_file({
       organizationId,
       workitemId: "TXRP-595",
       id: attachment.id
     })
     
     // 使用 curl 下载
     await executeBash(`curl -L -o "${attachment.name}" "${downloadUrl}"`)
     
     // 查看下载的文件
     await readFile(attachment.name)
   }
   ```

5. **分析截图**：检查截图中显示的视觉问题

6. **继续修复**

## 示例 7：容错的状态更新

**用户输入：**
```
将 TXRP-592 更新为"开发完成"
```

**Agent 工作流：**

1. **读取当前工作项**：
   ```javascript
   const workItem = await mcp_aliyun_yunxiao_get_work_item({
     organizationId,
     workItemId: "TXRP-592"
   })
   ```

2. **尝试状态更新**：
   ```javascript
   try {
     await mcp_aliyun_yunxiao_update_work_item({
       organizationId,
       workItemId: "TXRP-592",
       updateWorkItemFields: {
         status: "100011" // 开发完成
       }
     })
   } catch (error) {
     // 状态 ID 可能对这个项目不正确
   }
   ```

3. **如果失败，查询工作流**：
   ```javascript
   const workflow = await mcp_aliyun_yunxiao_get_work_item_workflow({
     organizationId,
     workItemId: "TXRP-592"
   })
   
   // 找到"开发完成"的正确状态 ID
   const devCompleteStatus = workflow.statuses.find(
     s => s.name === "开发完成" || s.name.includes("开发完成")
   )
   ```

4. **使用正确的 ID 重试**：
   ```javascript
   await mcp_aliyun_yunxiao_update_work_item({
     organizationId,
     workItemId: "TXRP-592",
     updateWorkItemFields: {
       status: devCompleteStatus.id
     }
   })
   ```

5. **添加评论**：记录状态变更

## 示例 8：完整的修复流程

**用户输入：**
```
我已经确认 TXRP-592 的修复
```

**Agent 工作流：**

1. **更新状态**：
   ```javascript
   await mcp_aliyun_yunxiao_update_work_item({
     organizationId,
     workItemId: "TXRP-592",
     updateWorkItemFields: {
       status: "100011" // 开发完成
     }
   })
   ```

2. **添加详细评论**：
   ```javascript
   await mcp_aliyun_yunxiao_create_work_item_comment({
     organizationId,
     workItemId: "TXRP-592",
     content: `✅ 已修复 | ⏱️ 修复时间：2026-04-28 14:30
❗ 感谢测试同学 @张三 精准报 Bug！
⭐ 测试人，YYDS！

**⬇️ 验证步骤**
1. 打开移动浏览器（iOS Safari）
2. 进入登录页面
3. 点击登录按钮
4. 应该看到加载指示器并成功登录

**⚠️ 问题原因**
登录按钮的 CSS z-index 设置过低，导致点击事件
被遮罩层元素拦截，在移动设备上表现为无响应。

**⚡ 修复方案**
- 将登录按钮 z-index 从 1 提升到 100
- 给遮罩层添加 pointer-events: none
- 在 iOS Safari、Chrome mobile 和 Android 浏览器上测试

**➡️ 涉及文件**
\`src/components/LoginButton.vue\`
\`src/styles/mobile.css\``
   })
   ```

3. **确认成功**：向用户显示确认信息

## 最佳实践示例

### 渐进式披露
- 只在需要时加载必要信息
- 获取完整详情仅在用户确认后
- 按需下载附件

### 容错机制
- 状态更新失败时自动查询工作流
- 使用正确的状态 ID 重试
- 优雅处理缺失的附件

### 用户确认
- 修复前展示 Bug 信息
- 应用更改前展示修复计划
- 更新云效前确认

### 完整上下文
- 始终获取评论（可能包含重要信息）
- 始终检查附件
- 始终下载并查看截图

### 结构化输出
- 使用一致的 Markdown 格式
- 包含所有相关信息
- 验证步骤清晰可操作
